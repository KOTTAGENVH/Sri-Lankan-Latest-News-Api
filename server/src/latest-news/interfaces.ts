export interface LankadeepaArticle {
  title: string;
  description?: string;
  source: string;
  image?: string;
  time?: string;
};

export interface BBCSinhalaArticle {
  title: string;
  source: string;
  image?: string;
  dateISO?: string;
};

export interface DeshayaArticle {
  title: string;
  description?: string;
  source: string;
  time?: string;
};
