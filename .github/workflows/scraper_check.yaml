name: Scraper Health Check

on:
  schedule:
    - cron: "30 18 * * *" # 18:30 UTC = 12:00 AM IST
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest

    steps:
      - name: Check Lankadeepa
        run: |
          curl -fs https://lankanewsapi.nowenkottage.com/latest-news/lankadepa/v1/1/1 \
          | jq -e '(.latestContent // []) | length > 0'

      - name: Check Deshaya
        run: |
          curl -fs https://lankanewsapi.nowenkottage.com/latest-news/deshaya/v1/1 \
          | jq -e '(.latestContent // []) | length > 0'

      - name: Check BBC Sinhala
        run: |
          curl -fs https://lankanewsapi.nowenkottage.com/latest-news/bbcsinhala/v1/3 \
          | jq -e '(.latestContent // []) | length > 0'

      - name: Check NewsFirst Tamil
        run: |
          curl -fs https://lankanewsapi.nowenkottage.com/latest-news/newsfirsttamil/v1 \
          | jq -e '(.latestContent // []) | length > 0'

      - name: Check NewsWire
        run: |
          curl -fs https://lankanewsapi.nowenkottage.com/latest-news/newswire/v1 \
          | jq -e '(.latestContent // []) | length > 0'

      - name: Check AdaDerana
        run: |
          curl -fs https://lankanewsapi.nowenkottage.com/latest-news/adaderana/v1 \
          | jq -e '(.latestContent // []) | length > 0'
